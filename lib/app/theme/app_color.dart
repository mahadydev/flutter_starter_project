import 'dart:ui';

/// Centralized application color palette.
///
/// Defines shared colors as well as light and dark theme color variants
/// used throughout the app UI.
class AppColors {
  AppColors._(); // Private constructor to prevent instantiation.

  /// This is the dominant blue color used throughout the site for buttons
  /// links, and primary UI elements
  static const Color primary = Color(0xFF2563EB);
}
